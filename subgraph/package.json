{
  "name": "crowdfund-subgraph",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ crowdfund-staking-sepolia",
    "create-local": "graph create --node http://localhost:8020/ crowdfund-staking-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ crowdfund-staking-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 crowdfund-staking-sepolia",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0",
    "@graphprotocol/graph-ts": "^0.38.0",
    "matchstick-as": "^0.6.0"
  }
}
